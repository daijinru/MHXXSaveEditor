# 2026-08-12

## MHXXSaveEditor 改造：C# WinForms → AI 驱动的逆向研究平台

用户要求把原《怪物猎人 XX》3DS 存档编辑器改造成 AI 驱动的研究平台（不绑定特定 LLM）。

**已完成：**
- `tools/extract.py`：从 C# 源码提取 43 个偏移 + 67 张 ID 映射表（21,098 条目）→ `data/offsets.json`、`data/game_constants.json`（保留注释，C# 源码为唯一权威源）
- `webapp/server.py`：Python 标准库 http.server 后端，API：save/load|upload|bytes|hex|patch|write|diff、offsets、constants、experiment(s)、knowledge
- `webapp/static/`：原生 HTML/JS/CSS 三栏界面（偏移表 | Hex 查看器 | 研究面板），无框架
- `tools/hexdump.py`、`tools/patch.py`、`tools/diff.py`：CLI 工具
- `tests/save_test.bin`：4,726,152 字节测试存档 + 端到端 API 验证通过
- 提交 `995a074`（12 文件，23,022 行），仓库级 git 身份：daijinru <daijinru@users.noreply.github.com>

**任务 #2 完成（13:0x）：**
- `docs/REVERSE_ENGINEERING.md`：研究闭环方法论（假设→实验→证据→验证→固化 + 证据分级 ⚪🟡🟢🔵 + 研究纪律）、快速上手、已知格式知识库（头部/角色/大区块结构，角色数据区标注为推断）、API/CLI 完整参考、实验记录规范、9 个待研究问题 Backlog
- 文档所有偏移数值已脚本化核对与 offsets.json 一致
- 已推送远程（用户操作）

**任务 #4 完成（15:4x）：**
- `.workbuddy/skills/mhxx-reverse-research/SKILL.md`：WorkBuddy 兼容技能（agent_created），AI 进驻工作流：前置确认 → 读 REVERSE_ENGINEERING.md → 启动服务+加载存档 → 选研究起点 → 五步研究闭环 → 记录实验 → 固化知识；含研究纪律、API/CLI 速查、常见陷阱（JSON 不能写 0x 字面量 / uint32 LE 字节序 / 写回前备份 / 动态验证需人类配合）
- 提交 `ddb2a52`（2 文件 501 行），连同上次未提交的 REVERSE_ENGINEERING.md 一起入库
- `.workbuddy/` 被 .gitignore 忽略，用 `git add -f` 强制跟踪 skill 目录使其随仓库分发
- 经 skill-creator 的 package_skill.py 验证通过（纯文档零脚本）

**使用流程演示（18:5x）：**
- 用测试存档完整演示 启动→加载→假设→实验(patch)→证据(diff)→记录实验→固化知识 全链路
- 发现 `data\..\experiments` 路径显示问题的根因：server.py 修复代码已随 995a074 提交，但运行中的是修复前启动的旧进程；重启后新代码生效，实验路径恢复 `experiments\...`
- 演示残留（experiments/*.md、knowledge 演示行）已全部清理，知识库恢复干净

**踩过的坑：**
- JSON 不支持 `0x24` 字面量，API 参数需用十进制或用 hex 字符串
- Python `int(x, 0)` 对已解析 int 报错，server 的 offset 解析需兼容 int/str
- 重启服务前旧进程未杀干净会导致端口被旧代码占用（用 PowerShell Stop-Process 杀）
- `.workbuddy/` 在 .gitignore 里（第 283 行），项目级 skill 需 `git add -f` 才能随仓库分发

**状态：5 个任务全部完成，提交 `995a074` + `ddb2a52`（本地，尚未推送）**
